export default function App() {
    return (
        <form>
            <input type="text" />
            <button type="submit">submit</button>
        </form>
    );
    //in above we are using (),as it has many tags.
}